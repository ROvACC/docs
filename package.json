{
  "name": "rovacc-docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "honkit build ./src ./build --log=debug",
    "serve": "honkit serve ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "honkit": "^4.0.0"
  }
}
